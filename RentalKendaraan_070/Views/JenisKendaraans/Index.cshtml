@model IEnumerable<RentalKendaraan_070.Models.JenisKendaraan>

@{
    ViewData["Title"] = "Index";
}

<h2>Tambahan Data Jenis Kendaraan</h2>

<p>
    <a asp-action="Create">Tambah Data Baru</a>
</p>

@using (Html.BeginForm("Index", "JenisKendaraans", FormMethod.Get))
{
    <p>
        NamaJenisKendaraan: @Html.DropDownList("ktsd", "Pilih Nama")
        Cari Data : @Html.TextBox("searchString")
        <input type="submit" value="Filter" />
    </p>
}
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.NamaJenisKendaraan)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
        @foreach (var item in Model)
        {
            <tr>
                <td>
                    @Html.DisplayFor(modelItem => item.NamaJenisKendaraan)
                </td>
                <td>
                    <a asp-action="Edit" asp-route-id="@item.IdJenisKendaraan">Edit</a> |
                    <a asp-action="Details" asp-route-id="@item.IdJenisKendaraan">Details</a> |
                    <a asp-action="Delete" asp-route-id="@item.IdJenisKendaraan">Delete</a>
                </td>
            </tr>
        }
    </tbody>
</table>
